<Window x:Class="Dolgova.ProductCatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dolgova"
        mc:Ignorable="d"
        Title="Каталог продукции" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" >
            <TextBlock Text="Тип продукции:" VerticalAlignment="Center" Margin="0,0,10,0" FontFamily="Bahnschrift Light SemiCondensed"/>
            <ComboBox x:Name="cbProductType" Width="200" SelectionChanged="CbProductType_SelectionChanged"
                      DisplayMemberPath="Name" SelectedValuePath="ID" FontFamily="Bahnschrift Light SemiCondensed"/>
        </StackPanel>
        <DataGrid x:Name="productsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" Margin="0,0,0,35" Background="#FFBBDCFA" FontFamily="Bahnschrift Light SemiCondensed">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Total}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Назад" Width="100" Height="30" Click="BtnBack_Click" Background="#FF015C99" Foreground="White" FontFamily="Bahnschrift Light SemiCondensed"/>
        </StackPanel>
    </Grid>
</Window>
